# Design Tokens

Tokens are generated from `tailwind.config.js`.

```ts
import { designTokens } from '../index';
```

Use token names like `color-primary-base` or `spacing-4` to keep styles consistent.
